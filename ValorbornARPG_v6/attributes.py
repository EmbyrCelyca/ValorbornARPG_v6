class Attributes:
    def __init__(self):
        # Define common attributes for various skills
        self.move_speed = 5
        self.cast_speed = 1.0  # Adjust as needed (e.g., higher values for faster casting)
        self.skill_duration = 0.5  # Adjust as needed (e.g., longer values for longer skill effects)
